# Use a modern FastAPI to match your existing Starlette 1.0.0
fastapi>=0.115.0 

# Keep your existing high-version packages to prevent breaking Streamlit
uvicorn>=0.47.0
websockets>=16.0
opencv-python>=4.13.0
numpy>=2.4.6
face-recognition==1.3.0

# Explicitly maintain Streamlit so pip doesn't touch it
streamlit>=1.57.0